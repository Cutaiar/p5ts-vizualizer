{
  "name": "p5ts-vizualizer",
  "version": "1.0.0",
  "description": "visualize something",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project ./tsconfig.json",
    "start": "npm run build && browser-sync start --server -w"
  },
  "keywords": [
    "p5",
    "ts",
    "visualizer"
  ],
  "author": "Cutaiar",
  "license": "ISC",
  "devDependencies": {
    "@types/p5": "^0.9.1",
    "typescript": "^4.0.5",
    "browser-sync": "2.26.12"
  }
}
